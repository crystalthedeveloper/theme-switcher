Webflow.push(function () {const root = document.body;const userPref = localStorage.getItem("theme");const systemPref = window.matchMedia("(prefers-color-scheme: dark)").matches;const theme = userPref || (systemPref ? "dark" : "light");root.setAttribute("data-theme", theme);applyTheme(theme);window.matchMedia("(prefers-color-scheme: dark)").addEventListener("change", e => {if (!localStorage.getItem("theme")) {const newTheme = e.matches ? "dark" : "light";root.setAttribute("data-theme", newTheme);applyTheme(newTheme);}});function toggleTheme() {const currentTheme = root.getAttribute("data-theme");const newTheme = currentTheme === "dark" ? "light" : "dark";root.setAttribute("data-theme", newTheme);localStorage.setItem("theme", newTheme);applyTheme(newTheme);}function applyTheme(theme) {updateIcons(theme);updateTextColors(theme);updateBackgrounds(theme);updateLinkColors(theme);updateImages(theme);}function updateIcons(theme) {const darkIcons = document.querySelectorAll("[data-icon='dark']");const lightIcons = document.querySelectorAll("[data-icon='light']");darkIcons.forEach(icon => {icon.style.opacity = theme === "dark" ? "1" : "0";icon.style.transform = theme === "dark" ? "scale(1)" : "scale(0.8)";icon.style.pointerEvents = theme === "dark" ? "auto" : "none";});lightIcons.forEach(icon => {icon.style.opacity = theme === "light" ? "1" : "0";icon.style.transform = theme === "light" ? "scale(1)" : "scale(0.8)";icon.style.pointerEvents = theme === "light" ? "auto" : "none";});}function applyColor(el, theme, type) {const customColor = el.getAttribute(`data-${theme}`);if (customColor) {el.style[type] = customColor;} else {if (type === "color") el.style.color = theme === "dark" ? "#ffffff" : "#000000";if (type === "backgroundColor") el.style.backgroundColor = theme === "dark" ? "#000000" : "#ffffff";}el.style.transition = `${type} 0.3s ease`;}function updateTextColors(theme) {const elements = document.querySelectorAll("[data-theme-text]");elements.forEach(el => applyColor(el, theme, "color"));}function updateBackgrounds(theme) {const elements = document.querySelectorAll("[data-theme-bg]");elements.forEach(el => applyColor(el, theme, "backgroundColor"));}function updateLinkColors(theme) {const elements = document.querySelectorAll("[data-theme-link]");elements.forEach(el => applyColor(el, theme, "color"));}function updateImages(theme) {const darkImages = document.querySelectorAll('[data-theme-img="dark"]');const lightImages = document.querySelectorAll('[data-theme-img="light"]');darkImages.forEach(img => {img.style.display = theme === "dark" ? "block" : "none";img.setAttribute("aria-hidden", theme === "dark" ? "false" : "true");});lightImages.forEach(img => {img.style.display = theme === "light" ? "block" : "none";img.setAttribute("aria-hidden", theme === "light" ? "false" : "true");});}const toggle = document.querySelector("[data-theme-toggle]");if (toggle) {toggle.addEventListener("click", toggleTheme);}});